before:
  hooks:
    - go mod tidy

builds:
- env:
  - CGO_ENABLED=0
  goos:
    - darwin
    - linux
  goarch:
    - amd64

archives:
  - files:
    - LICENSE
    - README.md
    - completions/**/*

checksum:
  name_template: "checksums.txt"

snapshot:
  name_template: "{{ .Tag }}-next"

release:
  prerelease: true

changelog:
  sort: asc
  filters:
    exclude:
    - "^docs:"
    - "^test:"
